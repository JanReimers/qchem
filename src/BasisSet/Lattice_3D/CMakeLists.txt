# src/BasisSet/Lattice_3D — plane-wave (3D periodic) basis set.
#
# Placeholder: to be populated when the plane-wave calculation is implemented
# (see doc/PlaneWavePlan.md).  Wire in via `add_subdirectory(Lattice_3D)` in the
# parent CMakeLists once there are sources here.
